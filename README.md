The repository root respresents $HOME (sort of); most files and directories
here belong in the user's home directory.

The directory `root` represents `/`, used for system-wide settings (often
hardware-related).

